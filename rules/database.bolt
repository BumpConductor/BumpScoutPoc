path /solutions {
  read() { true }
  write() { isSignedIn() }
}

path /solvers {
  read() { true }
  write() { isSignedIn() }
}

path /bumps {
  read() { true }
  write() { isSignedIn() }
}

isSignedIn() { auth != null }
